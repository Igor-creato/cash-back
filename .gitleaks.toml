title = "Cashback plugin gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Paths excluded from secret scanning (third-party / generated only)."
paths = [
    '''(^|/)vendor/''',
    '''(^|/)node_modules/''',
]
